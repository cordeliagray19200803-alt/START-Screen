# セキュリティ運用要件

この版は、医療情報をブラウザのlocalStorageへ残さず、Edge Functionで暗号化・復号し、データベースの直接公開を止める構成です。

## 本番公開前の必須事項

1. Supabaseのメール確認を有効化する。
2. 最低パスワード長を12文字以上に設定する。
3. Authのレート制限を確認し、可能ならCAPTCHAを有効化する。
4. `MEDICAL_DATA_KEY` と `AUDIT_HASH_KEY` をSupabase Secretsだけに保存する。
5. `ALLOWED_ORIGINS`を実際のGitHub Pages URLだけに限定する。
6. secret key / service_role keyをGitHubへ置かない。
7. SQLの実行後、anon/authenticatedで各テーブルを直接SELECTできないことを確認する。
8. 緊急表示停止、URL再発行、初期設定削除を実機で確認する。
9. バックアップ、鍵の保管、鍵のローテーション、事故時の連絡先を決める。
10. 一般利用者へ配布する前に第三者による脆弱性診断を行う。

## 鍵の管理

- `MEDICAL_DATA_KEY`: 32バイト以上のランダム値をBase64で設定。
- `AUDIT_HASH_KEY`: 32バイト以上のランダム値。
- GitHub、HTML、JavaScript、スクリーンショット、メールへ記載しない。
- 鍵を失うと既存データを復号できません。
- 鍵が漏れた可能性がある場合、公開停止、鍵交換、利用者通知、影響調査が必要です。

## ログ保持

- セキュリティ監査ログ: 90日を目安に削除。
- 緊急表示アクセスログ: 30日を目安に削除。
- ログには医療情報本文、氏名、メール、電話番号を保存しない。

## インシデント時

1. 緊急表示を一括停止する。
2. Edge Functionsを停止またはALLOWED_ORIGINSを閉じる。
3. 影響範囲とアクセス記録を確認する。
4. 必要に応じて個人情報保護委員会への報告と本人通知を行う。
5. 原因修正後に公開リンクを全件再発行する。
